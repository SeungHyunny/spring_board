<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vision.mybatis_board.dao.BMDao">

	<insert id="join">
		insert into member(id, pw, name, phone)
		values (#{param1}, #{param2, jdbcType=VARCHAR}, #{param3, jdbcType=VARCHAR}, #{param4, jdbcType=VARCHAR})
	</insert>

</mapper>